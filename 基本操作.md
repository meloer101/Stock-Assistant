python3 -m venv .venv
source .venv/bin/activate
pip install google-adk
pip install litellm

adk create my_agent

litellm作为转换层：
##########################################################################################
# LLM 设置
##########################################################################################
test_model = LiteLlm(
    model=os.getenv("LLM_MODEL"),
    api_key=os.getenv("LLM_MODEL_API_KEY"),
    # model="ollama/qwen3:0.6b",
    api_base=os.getenv('LLM_BASE_URL', {}),
    # api_base="http://192.168.31.130:11434"
    # api_base="http://192.168.31.130:11434/v1/chat/completions"
)

启动：
后端：
source /Users/m/Desktop/Vine-MVP/.venv/bin/activate
cd /Users/m/Desktop/Vine-MVP/backend
uvicorn main:app --reload --port 8000

前端：
source /Users/m/Desktop/Vine-MVP/.venv/bin/activate
cd /Users/m/Desktop/Vine-MVP/frontend
npm run dev